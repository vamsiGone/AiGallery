// app/_layout.tsx
import { Tabs } from 'expo-router';
import React from 'react';

export default function Layout() {
  return (
    <Tabs>
      {/* index, faces, duplicates, settings will map to app/index.tsx, app/faces.tsx, etc. */}
    </Tabs>
  );
}
